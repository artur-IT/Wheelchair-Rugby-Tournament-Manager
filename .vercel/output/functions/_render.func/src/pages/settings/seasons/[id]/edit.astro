---
import Layout from "@/layouts/Layout.astro";
import SeasonForm from "@/features/settings/components/SeasonForm/SeasonForm";

const { id } = Astro.params;
if (!id) return Astro.redirect("/settings");
---

<Layout title="Edytuj sezon">
  <SeasonForm id={id} client:load />
</Layout>
