# opcjonalny, jeśli generator „za bardzo kombinuje” — trzymanie się wymagań

Before generating any code, restate the requirements as a checklist and confirm you will follow them.
Then generate the code exactly as requested, without adding extra pages, extra tech, or JavaScript.
Do not omit any of the required HTML files. Keep the CSS in styles.css only.
