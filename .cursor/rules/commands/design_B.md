Create a lightweight design system for the above dashboard app (HTML/CSS only, no JS, no frameworks).
Style direction: modern sports admin dashboard (professional, clean, energetic accents).

Output:
- Color palette (CSS variables): background, surface, text, muted, border, primary, danger, success, warning.
- Typography scale (CSS variables) and recommended font stack (system fonts OK).
- Spacing scale (CSS variables).
- Components spec (visual + HTML structure guidelines):
  - topbar, sidebar, page header with breadcrumbs
  - buttons (primary, secondary, danger)
  - form fields (text, email, tel, textarea, select), fieldset/legend pattern
  - tables (striped + responsive handling)
  - cards
  - badges/status chips (e.g., “Draft”, “Scheduled”, “Finished”)
  - empty states
- Responsiveness rules:
  - breakpoints: 480px / 768px / 1024px
  - sidebar collapses on mobile into a topbar menu placeholder (since no JS, use a visible compact nav area)
- Accessibility requirements:
  - focus-visible styles
  - skip link
  - labels for inputs, proper headings hierarchy

UI text in Polish; code snippets and comments in English.
